// 아스키 코드로 변환하여 65 ~ 90 사이의 값을 찾는다.
// function solution(word) {
//   let count = 0;
//   for (let i in word) {
// if (word[i].charCodeAt() > 64 && word[i].charCodeAt() < 91) {
//   count += 1;
// }
//   }
//   return count;
// }

function solution(word) {
  let answer = 0;
  for (let i = 0; i < word.length; i++) {
    if (word[i] === word[i].toUpperCase()) {
      answer++;
    }
  }
  return answer;
}

console.log(solution("KoreaTimeGood"));
