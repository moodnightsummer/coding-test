function solution(word) {
  const answer =
    word.length % 2 === 1
      ? word.slice(Math.floor(word.length / 2), Math.floor(word.length / 2) + 1)
      : word.slice(word.length / 2 - 1, word.length / 2 + 1);
  return answer;
}

console.log(solution("gooouood"));
