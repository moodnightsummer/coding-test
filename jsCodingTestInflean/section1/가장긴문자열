function solution(word, num) {
  let answer = "";
  for (let i = 0; i < word.length; i++) {
    if (answer.length < word[i].length) {
      answer = word[i];
    }
  }
  return answer;
}

console.log(solution(["teacher", "time", "student", "beautiful", "good"], 5));
